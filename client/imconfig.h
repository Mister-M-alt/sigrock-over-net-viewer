// Dear ImGui user config for sonview.
#pragma once
// 32-bit vertex indices: dense waveforms + annotations can exceed the 16-bit
// default (65535 verts per draw list).
#define ImDrawIdx unsigned int
